#include <stdio.h>

/**
 * main - prints a statement using the funtion printf
 *
 * Return: 0 if the program exits successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
